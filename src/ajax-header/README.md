# ajax-header

This extension adds the `X-Requested-With: XMLHttpRequest` header to all htmx requests.

## Install

```html
<script src="/path/to/ext/ajax-header.js" defer></script>
```

## Usage

Once loaded, the header is automatically added to all htmx requests globally.
